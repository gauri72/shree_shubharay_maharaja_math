export const metadata = {
    title: 'Events — Shubharay Math',
    description: 'See upcoming events, satsangs and utsavs at Shri Shubharay Maharaj Math.',
}

export default function EventsLayout({ children }) {
    return children
}
